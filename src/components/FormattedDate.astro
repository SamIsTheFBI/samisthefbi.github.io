---
export interface Props {
  date: Date;
}

const { date } = Astro.props;
---
<time class="text-sm font-poppins" datetime={date.toISOString()}>
  {
    date.toLocaleDateString("en-EN", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>