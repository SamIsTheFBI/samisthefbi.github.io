name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  # JOB 1: Check if LaTeX files changed
  changes:
    runs-on: ubuntu-latest
    outputs:
      latex: ${{ steps.check.outputs.latex }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check for LaTeX changes
        id: check
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "latex=true" >> "$GITHUB_OUTPUT"
          elif git diff --name-only HEAD^ HEAD | grep -qE '\.(tex|sty|bib)$'; then
            echo "latex=true" >> "$GITHUB_OUTPUT"
          else
            echo "latex=false" >> "$GITHUB_OUTPUT"
          fi

  # JOB 2: Only rebuild PDF when LaTeX changed
  build_latex:
    needs: changes
    if: needs.changes.outputs.latex == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compile LaTeX
        uses: xu-cheng/latex-action@v4
        with:
          root_file: resume.tex

      - name: Update PDF in repository
        run: |
          mkdir -p public
          cp resume.pdf public/resume.pdf

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add public/resume.pdf

          if git diff --staged --quiet; then
            echo "PDF unchanged"
          else
            git commit -m "chore: update resume.pdf [skip ci]"
            git push
          fi

  # JOB 3: ALWAYS build Astro
  build:
    needs: [changes, build_latex]
    if: always() && needs.changes.result == 'success' && needs.build_latex.result != 'failure' && needs.build_latex.result != 'cancelled'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Astro site
        uses: withastro/action@v3

  # JOB 4: Deploy to GitHub Pages
  deploy:
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
